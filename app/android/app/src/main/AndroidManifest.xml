<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".OnDevAIApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SnapdragonLab">
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />
        <uses-native-library
            android:name="libcdsprpc.so"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".DebugReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="ai.ondev.snapdragonlab.DEBUG_RUN_BENCHMARK" />
                <action android:name="ai.ondev.snapdragonlab.DEBUG_PROBE_OPENCL" />
                <action android:name="ai.ondev.snapdragonlab.DEBUG_OPENCL_SMOKE" />
                <action android:name="ai.ondev.snapdragonlab.DEBUG_QNN_CONTROL_SMOKE" />
                <action android:name="ai.ondev.snapdragonlab.DEBUG_QNN_ENGINE_SMOKE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
